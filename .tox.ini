[tox]
envlist = py35

[testenv]
deps=
    pytest
    nose
    coverage
    six
    mr.bob
    pyreadline
    click
    PyYAML
    GitPython
commands=nosetests -w diamond_patterns -c etc/tests-travis.cfg --with-coverage --cover-package=diamond_patterns
